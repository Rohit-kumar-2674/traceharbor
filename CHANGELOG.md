# Changelog

## Unreleased

- Add `verify-export` for portable ZIP checksum verification without extraction or a live vault.
- Reject malformed manifests, duplicate/unexpected members, encrypted archives, and oversized exports.
- Document checksum-verification boundaries and add ten regression cases.
- Pin CI actions to reviewed commits, build distributable packages in CI, and enable dependency update proposals.
- Use the repository logo in the README instead of a third-party image.
- Modernize the existing MIT license metadata and add repository links to built packages.
